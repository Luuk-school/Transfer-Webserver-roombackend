apiVersion: v1
kind: Service
metadata:
  name: room-backend
spec:
  selector:
    app: room-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
